## 抠图/透明背景图

Rembg 是用于从图像中去除背景的python库，它使用机器学习模型自动检测并剔除背景，非常适合需要图像处理的项目。

它支持多种格式，并且能够处理复杂的边界情况，比如头发边缘、半透明物体等，这是传统的基于颜色的方法难以做到的。
通过 pip 进行安装：`pip install rembg`

安装完成后，你可以直接通过命令行来使用 rembg，去除一张图片的背景并保存为 PNG 格式（PNG 支持透明背景）：
`rembg i input.jpg output.png`

实际抠图蛮复杂的，没有上面这个命令行这么简单，需要写几十行的脚本才能完成。

